---
id: github-actions-workflows
sidebar_position: 7
title: GitHub Actions 工作流
description: 本目录包含用于自动化 CI/CD、测试和发布的 GitHub Actions 工作流。
last_update:
  author: Aurelius
  date: 2025-11-25
tags:
  - GitHub Actions
  - Workflows
  - CI/CD
  - Testing
  - Publish
---

## 架构概览

```mermaid
graph TD
    subgraph "触发源"
        A[推送到 main]
        C["标签 v*.*.*"]
        G[计划/手动]
        J[创建 PR]
    end

    subgraph "CI 流水线"
        B[ci.yml]
        B1[跨平台测试]
        B2[Lint & 类型检查]
        B3[安全审计]
        B4[构建验证]
        B5[覆盖率报告]
        B --> B1 & B2 & B3 & B4 & B5
    end

    subgraph "发布流水线"
        D[publish.yml]
        D0[调用 CI 验证]
        D1[构建分发包]
        D2[创建 GitHub Release]
        D3[发布到 TestPyPI]
        D4[发布到 PyPI]
        D5[更新 Changelog]
        D --> D0 --> D1 --> D2 & D3
        D3 -.-> D4 --> D5
    end

    A --> B
    J --> B
    C --> D
    G --> |依赖更新| H[dependencies.yml]
    J --> |代码审查| K[review.yml]

    style B fill:#2196F3,color:#fff
    style D fill:#4CAF50,color:#fff
    style H fill:#FF9800,color:#fff
    style K fill:#9C27B0,color:#fff
```

## 工作流清单

### 🔄 CI (持续集成) — [`ci.yml`](../.github/workflows/ci.yml)

**触发条件：** 推送到主分支、拉取请求、被其他工作流调用（`workflow_call`）、手动触发

**Jobs：**

| Job | 职责 |
|-----|------|
| `test` | 在 Ubuntu / Windows / macOS 上运行 pytest |
| `lint` | ruff lint + format check + mypy 类型检查 |
| `security` | bandit 静态安全扫描 + pip-audit 依赖漏洞扫描 |
| `build` | 构建 wheel 并验证安装 |
| `coverage` | 覆盖率报告 + Codecov 上传 |

**特性：** 支持 `workflow_call`，可被 publish.yml 作为验证步骤调用。

### 🚀 发布 — [`publish.yml`](../.github/workflows/publish.yml)

**触发条件：** `v*.*.*` 标签推送、Release 发布、手动触发

**Jobs：**

| Job | 职责 |
|-----|------|
| `validate` | 调用 ci.yml 执行完整验证 |
| `build` | 构建分发包 + twine check |
| `github-release` | 从 CHANGELOG 提取 release notes，创建 GitHub Release |
| `testpypi` | 发布到 TestPyPI（标签或手动触发） |
| `pypi` | 发布到 PyPI（仅 Release 事件） |
| `changelog-update` | 发布后更新 CHANGELOG |

**特性：** 使用 OIDC 可信发布，无需 API 密钥。

### 📋 依赖管理 — [`dependencies.yml`](../.github/workflows/dependencies.yml)

**触发条件：** 每周一 9:00 UTC、手动触发

**Jobs：**

| Job | 职责 |
|-----|------|
| `update` | `uv lock --upgrade` → 测试 → 创建 PR |

### 🤖 代码审查 — [`review.yml`](../.github/workflows/review.yml)

**触发条件：** PR 创建/更新、推送到 master/main、手动触发

**Jobs：**

| Job | 职责 |
|-----|------|
| `pr-review` | 审查 PR 变更文件，发布 PR 评论 |
| `push-review` | 审查推送到主分支的变更，发布 commit 评论 |

**共享资源：**
- [`review-prompt.txt`](../.github/review-prompt.txt) — 审查提示模板
- [`scripts/review-files.sh`](../.github/scripts/review-files.sh) — 文件审查循环脚本

## 可复用组件

### Composite Action: [`setup-python-uv`](../.github/actions/setup-python-uv/action.yml)

所有工作流共享的 Python 环境初始化 action。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `python-version` | `3.13` | Python 版本 |
| `install-dev` | `true` | 是否安装开发依赖 |
| `enable-cache` | `true` | 是否启用 uv 缓存 |

## 使用指南

### 创建版本

1. 确保 `pyproject.toml` 中版本号正确
2. 在 `CHANGELOG.md` 中添加版本说明（`## v{version}` 标题下）
3. 创建标签并推送：
   ```bash
   git tag v1.2.3
   git push origin v1.2.3
   ```
4. `publish.yml` 将自动执行完整发布流水线

### 环境配置

**仓库密钥：**

| Secret | 用途 | 必需 |
|--------|------|------|
| `ANTHROPIC_API_KEY` | 代码审查 | 仅 review.yml |
| `ANTHROPIC_BASE_URL` | API 端点 | 可选 |

**PyPI 可信发布：**

1. PyPI 账户 → 发布 → 添加待发布者
2. 填写：所有者、仓库名、工作流 `publish.yml`、环境 `pypi`

**GitHub 环境：**

- `pypi` — 生产环境 PyPI 发布
- `testpypi` — TestPyPI 发布（可选）

## 故障排除

| 问题 | 解决方案 |
|------|---------|
| 构建失败 | 检查 CI 日志中的测试/lint 失败 |
| 发布失败 | 验证 PyPI 可信发布配置与环境设置 |
| 依赖 PR 创建失败 | 检查 Actions 权限（Settings → Actions → Workflow permissions） |
| 覆盖率下降 | 为新代码添加测试 |
